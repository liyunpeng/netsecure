----------------------------------------------------------------------
可扩展性需要程序员自己来做， 不能外包给操作系统。 
硬件性能提高两倍， 软件的吞吐量规模不一定能提高两倍。
如何避免服务器性能随连接数增多而变差的问题。 
应该支持的并发数量的计算： 假如一个事务10秒， 每秒接受1000个事务， 那么必须支持1万个并发连接

一个数据包过来， 需要遍历所有的数据包吗。 

----------------------------
集群的master , 机器重启， 只要ip不变， 没有其他的服务冲突， 如没有etcd服务启动， 正常情况下 ， 
k8s是正常启动， 用kubectl get no可以看到ready状态。
如果ip地址变化了 ， 证书就失效了， 需要两个动作， 
一是把所有的。conf配置文件中的地址，替换成新地址， 
另一个是重新生成证书文件。 
这一步没成功。 