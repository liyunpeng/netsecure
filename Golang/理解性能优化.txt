为减少垃圾回收压力的编程注意：多个小对象，放在一个大对象，减少垃圾扫描。 

map的key能用int, 尽量不用string, 因为string是指针， 需要gc检测是否在堆上， 
无法像c语言直接用malloc，指定在堆中分配内存， 就算是new这样的操作也绝对分配在堆上 ， 
优化器判断是否要在堆上，是根据是否函数外部引用了本函数的变量。 

尽量不被外部引用，避免逃逸到堆上。 