结构体的大小计算：
先明确一下， 基本类型为nil时的长度。
一般int的长度和指针的长度都是机器的长度， 
所以在64位机器上:

int和指针的长度都是64位， 即8个字节， 

byte为1个字节

接口类型为16个字节， 因为描述接口的结构体内部有2个成员 ， 每个成员都是8个字节。

切片类型为24个字节， 因为描述切片的结构体有3个成员你， 每个成员为8个字节

函数变量和map变量，因为两者都是维护一个指针， 所以都是8个字节。 

有了基本类型的基础后，也就可以计算结构体的长度， 