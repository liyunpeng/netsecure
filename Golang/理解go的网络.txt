go 写后台最舒服的就是能够以同步写代码的方式操作网络, 但是网络操作不阻塞线程.主要是结合了非阻塞的 fd, epoll 以及协程的切换和恢复.linux 提供了网络 fd 的非阻塞模式, 对于没有 ready 的非阻塞 fd 执行网络操作时, linux 内核不阻塞线程, 会直接返回 EAGAIN, 这个时候将协程状态设置为 wait, 然后 m 去调度其他协程.
go 在初始化一个网络 fd 的时候, 就会把这个 fd 使用 epollctl 加入到全局的 epoll 节点中. 

解决同步荣来数据库的若干问题
	spacerace汇总转移记录增加产品名，用户名
	修改荣来特别奖励记录关联不到的问题
	不向荣来转移转入用户
	同步荣来数据库若干表
	修改荣来矿池收益记录
	转出方有提现未完成的订单， 禁止转移
	转入用户不要同步到荣来数据库，保证荣来整体算力正确
	修改同步到荣来数据库的用户算力